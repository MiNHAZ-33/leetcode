const mySqrt = (x) => {
    if (x == 1) return 1;
    let left = 0, right = x / 2;
    while (left < right) {
        const mid = Math.floor((left + right + 1) / 2);

    }

}


console.log(mySqrt(4)); // 2
